import { test } from '@playwright/test'

// Пока нет реальных E2E-сценариев — они появятся вместе с экранами (см. spec/tasks/26).
// Задача этого файла — подтвердить, что раннер Playwright настроен и запускается.
test.skip('placeholder — real E2E scenarios land in later tasks', () => {})
