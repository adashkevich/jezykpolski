# Трекер прогресса

> Обновляй этот файл при завершении каждой задачи.
> Формат отметки: `[ ]` не начато · `[~]` в работе · `[x]` готово · `[!]` заблокировано

---

## Как пользоваться

1. Задачи идут по порядку номеров. Зависимости указаны в шапке каждой задачи.
2. Задача считается готовой, только когда выполнены **все** пункты её Acceptance.
3. После каждой задачи должно выполняться: `npm run build` без ошибок и `npm run lint` без ошибок.

---

## Фаза 0 — Фундамент

| # | Задача | Статус | Зависит от |
|---|---|---|---|
| 01 | [Настройка тулчейна](01-toolchain-setup.md) | [x] | — |
| 02 | [Контентный пайплайн](02-content-pipeline.md) | [x] | 01 |
| 03 | [Доменная модель навыков](03-domain-model.md) | [x] | 02 |
| 04 | [Слой доступа к контенту](04-content-access-layer.md) | [x] | 02, 03 |
| 05 | [Dexie: схема и репозитории](05-persistence.md) | [x] | 03 |
| 06 | [App shell, роутинг, PWA](06-app-shell-pwa.md) | [x] | 01 |

**Результат фазы:** устанавливаемая оболочка приложения с пустыми экранами, загруженным индексом слов и рабочей БД.

---

## Фаза 1 — Vocabulary (ядро MVP)

| # | Задача | Статус | Зависит от |
|---|---|---|---|
| 07 | [Список слов](07-words-list.md) | [x] | 04, 05, 06 |
| 08 | [Карточка слова](08-word-detail.md) | [x] | 04, 05, 07 |
| 09 | [Движок упражнений](09-exercise-engine.md) | [x] | 03, 04 |
| 10 | [Генератор дистракторов](10-distractors.md) | [x] | 04, 09 |
| 11 | [FSRS-адаптер и политика SRS](11-srs.md) | [x] | 03, 05 |
| 12 | [Vocabulary-упражнения](12-vocabulary-exercises.md) | [x] | 09, 10 |
| 13 | [Сборка очереди и runner сессии](13-session-runner.md) | [x] | 09, 11, 12 |
| 14 | [Результаты сессии и режим ошибок](14-session-results.md) | [x] | 13 |
| 15 | [Главный экран](15-home-screen.md) | [x] | 11, 13 |
| 16 | [Свайпы в списке слов](16-swipe-triage.md) | [ ] | 07, 11 |

**Результат фазы:** полноценный продукт для изучения лексики — критерии приёмки MVP из `requirements.md` §6.

---

## Фаза 2 — Существительные

| # | Задача | Статус | Зависит от |
|---|---|---|---|
| 17 | [Раздел существительных и таблица склонения](17-nouns-section.md) | [ ] | 08, 13 |
| 18 | [Упражнения на формы существительных](18-noun-exercises.md) | [ ] | 17 |
| 19 | [Режим Practice и экран настройки тренировки](19-practice-mode.md) | [ ] | 18 |

---

## Фаза 3 — Глаголы

| # | Задача | Статус | Зависит от |
|---|---|---|---|
| 20 | [Раздел глаголов и таблица спряжения](20-verbs-section.md) | [ ] | 17 |
| 21 | [Упражнения на формы глаголов](21-verb-exercises.md) | [ ] | 20, 19 |

---

## Фаза 4 — Прилагательные

| # | Задача | Статус | Зависит от |
|---|---|---|---|
| 22 | [Раздел прилагательных и степени сравнения](22-adjectives-section.md) | [ ] | 20 |

---

## Фаза 5 — Статистика, данные, качество

| # | Задача | Статус | Зависит от |
|---|---|---|---|
| 23 | [Экран статистики](23-stats.md) | [ ] | 14, 18 |
| 24 | [Настройки, экспорт/импорт, сброс](24-settings-backup.md) | [ ] | 05 |
| 25 | [Офлайн, кэширование, UX обновления](25-offline-update.md) | [ ] | 06, 02 |
| 26 | [Доступность, E2E, производительность](26-quality-a11y-e2e.md) | [ ] | все предыдущие |

---

## Фаза 6 — Будущее (вне MVP)

| # | Задача | Статус |
|---|---|---|
| 27 | [Контекстные предложения и анализ ошибок](27-context-and-error-analysis.md) | [ ] |

---

## Журнал решений

Сюда записывай отклонения от `spec/architecture.md` и причины — правило blueprint §36.19.

| Дата | Задача | Решение | Причина |
|---|---|---|---|
| 2026-09-01 | 01 | Добавлен `@tanstack/react-virtual` (отклонение от blueprint §31) | Список из 7998 слов не бандлится целиком (requirements.md §4); собственная виртуализация с динамической высотой — источник багов скролла, библиотека headless и ~4 КБ gz |
| 2026-09-01 | 01 | `baseUrl` убран из `tsconfig.app.json`/`tsconfig.json`, остался только `paths: {"@/*": ["./src/*"]}` | В TypeScript 6.0 `baseUrl` deprecated (`TS5101`), ломает `tsc -b`; `paths` резолвятся относительно расположения tsconfig и без него |
| 2026-09-01 | 01 | `paths` продублирован в корневом `tsconfig.json` (solution-файл) | shadcn CLI читает корневой tsconfig напрямую и не подхватывает `paths` из `tsconfig.app.json`, иначе генерирует файлы в буквальный каталог `./@/...` |
| 2026-09-01 | 01 | `react-refresh/only-export-components` отключено только для `src/components/ui/**/*` | Сгенерированные shadcn-компоненты (напр. `button.tsx`) экспортируют не только компонент, но и `buttonVariants` — это vendor-код, не авторский |
| 2026-09-01 | 01 | Vitest ограничен `src/**`, из его области исключён `e2e/**` | Vitest по умолчанию подхватывал `e2e/*.spec.ts` и падал на Playwright-специфичном `test.skip`; раннеры несовместимы, разделены по каталогам |
| 2026-09-01 | 01 | `e2e/placeholder.spec.ts` содержит один `test.skip(...)`, а не пустая директория | Буквально пустой `e2e/` даёт `Error: No tests found` и exit code 1 у Playwright; плейсхолдер даёт exit code 0 без реальных сценариев (появятся в задаче 26) |
| 2026-09-01 | 01 | `src/index.css` (стили шаблона Vite) удалён, а не оставлен рядом с `globals.css` | Содержал хардкод шаблона (`#root { width: 1126px }`), нарушал acceptance «не осталось файлов стартового шаблона» и запрет page-specific CSS (blueprint §8) |
| 2026-09-01 | 01 | `src/App.tsx` остался на месте (не перенесён в `src/app/App.tsx`) | Задача 01 требует только очистить содержимое; полноценный AppShell/роутинг по структуре `architecture.md §3` — предмет задачи 06 |
| 2026-09-01 | 02 | Осиротевшие парадигмы (`audi\|NOUN`, `bmw\|NOUN`) пишутся в свой шард парадигм, но исключены из `index.json` | Текст задачи говорит «игнорируются» про несовпадение со словом (нет строки в индексе); удаление самих форм сломало бы acceptance «сумма форм по шардам = 195 487», которая эти 42 формы включает |
| 2026-09-01 | 02 | `manifest.generatedAt` берётся из `generated_at` исходных `data/*.json`, а не из `new Date()` во время сборки | Иначе невозможно выполнить acceptance «повторный запуск даёт побайтово идентичный результат» — `manifest.json` не был бы детерминирован |
| 2026-09-01 | 02 | Все словари кодеков (включая `pos`/`level`) — 1-based, `0` зарезервирован под «нет значения» | Единая модель на все десять словарей вместо особого случая для тех двух, где значение никогда не отсутствует |
| 2026-09-01 | 02 | Один общий словарь `GENDER` (10 значений) на NOUN/VERB/ADJ вместо трёх раздельных | Множества значений рода существенно пересекаются между частями речи (masculine_personal, feminine, neuter встречаются во всех трёх) |
| 2026-09-01 | 02 | Шард для `senses/NNN.json` тоже выбирается тем же FNV-1a хэшем от `wordId` (как и для парадигм), хотя спека явно требует стабильный хэш только для парадигм | Согласованность и то же свойство «номер шарда не меняется между сборками», которое явно проверяется acceptance только для парадигм, но полезно и для senses |
| 2026-09-01 | 02 | Добавлен `tsconfig.scripts.json`, не упомянутый в спеке, подключён через `references` в корневой `tsconfig.json` | `scripts/build-content.ts` получает тот же strict-режим TS, что и остальной код, под общим `tsc -b` |
| 2026-09-01 | 03 | Добавлен `src/types/content.ts` с типами `WordIndexEntry`/`Paradigm` (не в списке файлов задачи 03) | `enumerateSkills` нуждается в декодированных типах контента, а слоя доступа к контенту (задача 04) ещё нет; типы переиспользуют `@/content/codec.ts`, задача 04 сможет их импортировать, а не задавать обратную зависимость |
| 2026-09-01 | 03 | Глагольный инфинитив не порождает отдельный навык | В пространстве измерений §5.1 нет `verb:infinitive`; голая форма инфинитива — это лемма, уже покрыта `vocab:pl-ru`/`vocab:ru-pl` |
| 2026-09-01 | 03 | `adj:<number>:<gender>:<case>` строится только из форм `degree: positive`; сравнительная/превосходная степень даёт только `adj:degree:<degree>` из формы-цитаты (sg, nominative, голый masculine) | Соответствует примеру app-design.md §14 (dobry → lepszy/najlepszy); полностью просклонённые формы сравнительной степени намеренно не становятся альтернативными ответами того же слота — это разные значения |
| 2026-09-01 | 03 | Позитивная степень ADV не порождает навык | Та же причина, что для инфинитива глагола — избыточна относительно `vocab:*` |
| 2026-09-01 | 03 | `SessionRecord`/`DailyStatsRecord` — состав полей придуман в этой задаче (architecture.md §8 объявляет их только как непрозрачные `Table<...>`) | Ориентировался на app-design.md §21 (результаты сессии) и §26 (статистика); задокументировано в шапке `types/progress.ts`, задача 05 может пересмотреть при заведении Dexie-схемы |
| 2026-09-01 | 03 | `Rating` определён локально в `types/progress.ts` как `1\|2\|3\|4`, а не импортирован из `ts-fsrs` | `ts-fsrs` — только в `learning/srs/fsrs-adapter.ts` (задача 11), это единственная точка импорта по правилу architecture.md |
| 2026-09-01 | 04 | В `src/types/content.ts` добавлен тип `Sense` (задача 03 его не определяла) | Декодированное представление значения слова нужно `content/senses.ts`; задача 09+ (движок упражнений) будет использовать его же |
| 2026-09-01 | 04 | Мемоизация `queryWords` двухуровневая: `WeakMap` по индексу + отдельный `WeakMap` по карте прогресса для запросов с фильтром `status` | Прогресс — внешние изменяемые данные; ключ только по сериализованному запросу отдавал бы устаревший результат после изменения прогресса |
| 2026-09-01 | 04 | `LoadingScreen`/`ErrorState` размещены в `src/app/providers/`, а не в `src/components/app/` | `architecture.md §3` не перечисляет эти компоненты явно; задача 06 (AppShell) может перенести их при необходимости |
| 2026-09-01 | 04 | `getParadigm` бросает исключение для `wordId`, отсутствующего в индексе вообще, и возвращает `null` только для документированных 14 слов без парадигмы | Неизвестный id — ошибка вызывающего кода, а не пробел в данных; смешивать эти два случая в одном `null` скрыло бы баги |
| 2026-09-01 | 04 | Перф-тесты (acceptance п.1-2) и тест «до уровня B1» используют синтетический индекс из 7998 записей с реальным распределением по уровням, а не читают `public/content/index.json` с диска | В `src/**` нет доступа к Node `fs` (только `tsconfig.scripts.json` его имеет); согласуется с существующим подходом `codec.test.ts` (реальные числа зашиты как литералы). Супервизор перепроверил ключевые числа (3903, парадигма kobieta) напрямую на сгенерированных артефактах — совпадают |
| 2026-09-01 | 05 | `applyAnswer` спроектирован как чисто транзакционный слой: принимает уже вычисленный `AnswerInput` (`nextSrsState`, `reviewLog`, `isNewSkill`, `nextWordProgress`), не импортирует и не вызывает `ts-fsrs` | Задача 05 зависит только от 03, а FSRS-адаптер — задача 11, зависящая от 03 И 05. На момент задачи 05 планировщика ещё не существует; вычисление переносится на вызывающую сторону (задача 11), `applyAnswer` только атомарно пишет. Решение принято супервизором до запуска агента |
| 2026-09-01 | 05 | `nextWordProgress` вычисляется ДО открытия транзакции и передаётся готовым, а не считается внутри `applyAnswer`/`recomputeAll` | Вычисление дёргает `content/paradigms.ts#getParadigm`, который может уйти в сеть; держать открытой `readwrite`-транзакцию IndexedDB через `fetch()` небезопасно — транзакция может автокоммититься раньше, чем резолвится сеть |
| 2026-09-01 | 05 | `openDatabase()`/`deleteDatabase()` вынесены из `database.ts` в отдельный `lifecycle.repository.ts` | Позволило сделать ESLint-запрет на импорт `db/database` вне `src/db/**` без исключений — `database.ts` теперь экспортирует только схему/синглтон, наружу вообще ничего импортировать не нужно |
| 2026-09-01 | 05 | `WordProgressRecord.morphMaturity` для 14 слов без парадигмы хранится как `0`, а не `undefined` | Тип поля — обязательный `number` (задача 03); отличить «нет морфологии» от «0% владения морфологией» при необходимости придётся через отдельную проверку наличия парадигмы в контенте |
| 2026-09-01 | 05 | `recomputeAll` не оборачивает весь цикл пересчёта в одну `db.transaction(...)`, в отличие от `applyAnswer` | Та же опасность сети внутри транзакции: сначала все данные читаются и считаются обычными `await`, затем одна короткая безсетевая `readwrite`-транзакция (`clear` + `bulkPut`) на запись |
| 2026-09-01 | 06 | `LoadingScreen`/`ErrorState` — канонические в `src/components/app/`, `app/providers/*` (задачи 04/05) обновлены импортировать их оттуда | Соответствует architecture.md §3 («components/app/ — AppShell, BottomNav, PageContainer, EmptyState…»); дублирующих версий не осталось |
| 2026-09-01 | 06 | Precache-бюджет прод-сборки — 541.2 КБ gz против целевых ~500 КБ (architecture.md §11) | Превышение целиком из данных (`content/senses/*` 415 КБ + `index.json` 126 КБ), не из кода приложения; `spec/tasks/25-offline-update.md` §1 явно предвидит этот случай и относит решение (перевод части senses в runtime-кэш) к задаче 25 — задача 06 реализована буквально по своему тексту (manifest+index+senses в precache, paradigms исключены) |
| 2026-09-01 | 06 | Постоянный top-bar с иконкой «Настройки» встроен прямо в `AppShell`, а не через per-page `PageHeader.action` | Архитектурная диаграмма §9 показывает только `Outlet`+`BottomNavigation`, но явное продуктовое требование «Настройки — иконкой в шапке» на каждой странице надёжнее гарантировать на уровне шелла, чем полагаться, что каждая будущая страница добавит её сама |
| 2026-09-01 | 06 | `/nouns`, `/verbs`, `/adjectives` — отдельные маршруты (как в architecture.md §9), но не отдельные вкладки нижней навигации; переключение между ними — `PosSwitcher` внутри раздела «Слова» | Прямое указание задачи 06: устраняет дублирование четырёх почти одинаковых экранов списка |
| 2026-09-01 | 06 | Тёмная тема получила `@media (prefers-color-scheme: dark)` в дополнение к классу `.dark` | Без этого тёмная тема была физически недостижима — переключателя (задача 24) ещё нет, а системная тёмная тема раньше не подхватывалась вообще |
| 2026-09-01 | 06 | `scripts/generate-icons.ts` рендерит PNG-иконки сразу в целевом размере через `density`, без `.resize()` | Обойдён баг sharp/libvips: `.resize()` поверх растеризованного SVG стабильно портил крайние строки пикселей чёрной полосой (воспроизведено даже на тривиальном SVG) |
| 2026-09-01 | 07 | Кнопка «Учить» переходит на `/session`, передавая текущий фильтр через router state, без построения очереди/запуска упражнений | Движок упражнений (09), FSRS (11) и сборка очереди сессии (13) ещё не существуют — задача 07 зависит только от 04/05/06. Решение принято супервизором до запуска агента |
| 2026-09-01 | 07 | Persist фильтров реализован через `settings.repository.ts` (Dexie) с кастомным `PersistStorage`-адаптером для `zustand/middleware/persist`, а не через `localStorage` | Соответствует NFR-19 (весь durable state — через слой репозиториев); `settings` в architecture.md §8 спроектирована именно под пользовательские настройки. Гидрация асинхронна (~1 тик), это ок — `DatabaseProvider` уже держит приложение на LoadingScreen, пока БД не открыта |
| 2026-09-01 | 07 | `src/components/app/AppShell.tsx`: `min-h-svh` заменён на `h-svh` | Без ограничения высоты `overflow-y-auto` в `main` не включался, документ растягивался под весь контент; `@tanstack/react-virtual` измерял контейнер скролла как контент целиком и рендерил все 7998 строк вместо видимых — то, для чего вообще нужна виртуализация. Баг был скрыт до этой задачи: ни один прошлый экран не имел достаточно контента, чтобы его проявить |
| 2026-09-01 | 07 | Свайпы (FR-29) в список не добавлены | Явно не входят в scope задачи 07 (это задача 16, зависящая от 07); писать их сейчас означало бы трогать запись прогресса раньше времени |
| 2026-09-01 | 08 | Кнопка «Знаю» в карточке слова визуально есть (по макету app-design §4), но disabled с подсказкой «появится в задаче 16» — реальной SRS-логики не создано | Нужная константа `SWIPE_KNOWN_INITIAL_STABILITY` и вся политика — в `learning/srs/policy.ts` (задача 16 → 11, FSRS-адаптер), а задача 08 зависит только от 04/05/07. Изобретать локальный дубль константы значило бы рассинхрон с задачей 16. Решение принято супервизором до запуска агента |
| 2026-09-01 | 08 | Кнопка «Учить» в карточке слова — переход на `/session` с `{ wordId }` в router state, без построения очереди | Та же причина, что и для `LearnFab` (задача 07): движок сессии — задача 13. Форма state другая (`{ wordId }`, не `{ filter }}`), т.к. одно слово нельзя корректно выразить через `WordQuery` |
| 2026-09-01 | 08 | Род в шапке карточки существительного показывается только после раскрытия блока форм (не сразу, как в макете app-design §4 для `człowiek`) | Род — производная от `dominantGender` парадигмы, а acceptance п.9 прямо запрещает грузить парадигму до раскрытия блока форм; показать род раньше значило бы либо нарушить ленивую загрузку, либо задваивать источник данных |
| 2026-09-01 | 08 | В `content/paradigms.ts` добавлено поле `analytic: boolean` в `VerbConjugationRow` и исправлен баг сопоставления ADJ-форм с голым `masculine` (ранее такие формы не попадали ни в одну колонку таблицы, например `dobry` в Mianownik/Wołacz показывал «—» при реально существующей форме в данных) | `analytic` нужен для FR-44 («аналитические формы помечены»), которого в задаче 04 не было. Баг с `masculine` исправлен точечно в слое отображения (`paradigms.ts`), не тронуты `codec.ts`/`enumerate.ts`, чтобы не задеть уже протестированные подсчёты навыков |
| 2026-09-01 | 09 | `src/learning/exercises/distractors.ts` создан заранее с точными сигнатурами задачи 10 (`pickVocabDistractors`, `pickFormDistractors`), но с наивным телом (сид-детерминированная выборка без фильтрации по рангу/уровню/пересечению переводов) | Задача 10 зависит от задачи 09 (не наоборот), значит умного алгоритма дистракторов на момент 09 не существует; `generate.ts` вызывает именно эти два экспорта, так что задача 10 сможет заменить только тело функций, не трогая точку вызова. Решение принято супервизором до запуска агента |
| 2026-09-01 | 09 | Тест на acceptance «`grade` принимает `aborcji` и `aborcyj` для sg.gen» написан против реального слота — plural genitive, а не singular | Текст задачи 09 буквально говорит «sg.gen», но по реальным данным (`aborcja\|NOUN`, задача 03) альтернативные написания relates к множественному числу родительного падежа; тест против несуществующей комбинации был бы бессмысленным |
| 2026-09-01 | 09 | `Direction`/`SlotLabel`/`TableCell` — типы, упомянутые в architecture.md §7.1, но нигде не определённые — спроектированы в этой задаче: `Direction = 'pl-ru'\|'ru-pl'`, `SlotLabel = Dimension`, `TableCell = { slot, prefilled, accepted }` (по наброску таблицы в задаче 18 §4) | Спека ссылается на эти типы, не определяя их; синтез оставляет минимальную поверхность для будущих задач (12, 13, 18) |
| 2026-09-01 | 09 | `ExerciseInstance.id` — детерминированная строка `` `${skillId}::${seed}` ``, а не `crypto.randomUUID()`, как буквально сказано в architecture.md («uuid») | `crypto.randomUUID()` ломает acceptance «одинаковый seed → побайтово одинаковый результат»; строковый id того же назначения (уникален в рамках сессии) достаточен |
| 2026-09-01 | 09 | `grade()` бросает понятную ошибку на `table`/`matching` вместо попытки вычислить единый `correct`/`nearMiss` | У этих составных типов нет одного скалярного правильного ответа — каждая ячейка/пара оценивается отдельно (согласуется с описанием таблицы в задаче 18) |
| 2026-09-02 | 10 | `content/loader.ts` дополнен синхронными `peekSensesShard(n)`/`peekParadigmShard(n)` — читают уже разрешённые в памяти шарды, не инициируют fetch | `pickVocabDistractors`/`pickFormDistractors` обязаны быть синхронными (их зовёт `generate.ts` без await), а фильтр по пересечению переводов (FR-92) в общем случае требует данных из асинхронно загружаемых шардов senses; текст задачи 10 явно допускал решение «через уже загруженный в память кэш» |
| 2026-09-02 | 10 | Если шард переводов кандидата ещё не прогрет, исключение по пересечению переводов деградирует до сравнения только `primaryRu` (не полного множества значений) | Компромисс синхронности; задокументировано в коде и покрыто отдельным тестом. `wiedzieć`/`znać` всё равно корректно разделяются, т.к. делят именно `primaryRu`. **Требует внимания в задаче 13** (сборка очереди сессии): стоит прогревать шарды переводов кандидатов до вызова генератора дистракторов, иначе фильтр FR-92 иногда будет неполным |
| 2026-09-02 | 10 | Ослабление фильтра по рангу (шаг 5) идёt по лестнице множителей ×9, затем ×27, а не одним прыжком до «весь пул той же POS» | На реальных данных строгое окно ×3 недостаточно даже для частых слов, не только для редких C1/C2 (`mieć`, ранг 5, почти не имеет соседей в честном окне ×3); резкий прыжок к полному пулу POS впустил бы случайные далёкие слова вместо реально близких A1/A2-глаголов |
| 2026-09-02 | 11 | **Найден и исправлен реальный баг** в `eslint.config.js`: два блока `no-restricted-imports` (запрет React/Dexie в `learning/**` из задачи 03 и запрет прямого импорта `db/database` вне `src/db/**` из задачи 05) молча перекрывали друг друга для файлов, подпадающих под оба — flat-config ESLint полностью заменяет массив `patterns` по ключу правила при пересечении областей, не объединяет. На практике запрет React/Dexie в `learning/**` не работал вообще. Переструктурировано в 4 непересекающихся блока (`fsrs-adapter.ts` / остальной `learning/**` / `db/**` / всё прочее), каждый с полным набором своих паттернов. Супервизор подтвердил эмпирически: `import { useState } from 'react'` в `src/learning/skills/` теперь корректно ловится линтером (до фикса — нет) | Правило добавления `ts-fsrs`-изоляции (задача 11) легло бы на тот же конфликтующий ключ и усугубило проблему; без переструктуризации либо новое правило само не сработало бы, либо (что хуже) молча выключило бы старые |
| 2026-09-02 | 11 | В конфигурации `ts-fsrs` `fsrs()` явно задано `learning_steps: ['10m']` вместо дефолтных `['1m', '10m']` | `SrsState` (архитектура §6.1) не хранит FSRS-индекс шага обучения, поэтому при каждом восстановлении `SrsState → Card` этот индекс сбрасывается в 0. С дефолтной 2-шаговой последовательностью навык, стабильно отвечаемый на «Good», никогда не переходил из `learning` в `review` — воспроизведено регресс-тестом. Однош́аговая последовательность делает шаг 0 всегда последним: `Good`/`Easy` гарантированно выпускают карточку в `review`, `Again`/`Hard` по-прежнему держат в `learning`/`relearning` |
| 2026-09-02 | 12 | `SelfAssessExercise` принимает дополнительные обязательные пропы `srsState: SrsState` и `now: number` сверх базового `ExerciseProps<E>` | `previewIntervals(state, now)` (задача 11) нужен для показа предполагаемых интервалов на кнопках рейтинга (шаг 5 задачи 12), а SRS-состояние навыка не часть `Exercise`. `now` обязателен явным параметром, не `Date.now()` внутри рендера — компонент остаётся детерминированным, как и весь `fsrs-adapter.ts` |
| 2026-09-02 | 12 | `SelfAssessExercise` показывает 3 кнопки рейтинга («Не знаю / Трудно / Знаю»), не 4 | Буквально соответствует app-design.md §6 п.8 и FR-54; `Rating` в домене — `1\|2\|3\|4`, но самооценка пользователем `Easy` не предусмотрена продуктом |
| 2026-09-02 | 12 | `ExerciseFeedback` — отдельный компонент-сосед, не встроен в каждый тип упражнения; колбэка «Далее» внутри `ChoiceExercise`/`InputExercise`/`SelfAssessExercise` нет | Контракт `ExerciseProps<E>` из задачи буквально не содержит такого колбэка — управление тем, когда показывать фидбек и переходить дальше, отдано будущему runner'у (задача 13) |
| 2026-09-02 | 13 | Закрыт router state из задач 07/08: `{filter}` (LearnFab) сужает пул `candidateNewWords`/`dueSkills` через `queryWords`; `{wordId}` (карточка слова) ограничивает очередь навыками только этого слова | Обе кнопки были заведены заранее (задачи 07/08) как чистая навигация без построения очереди — движка сессии тогда не существовало. Задача 13 — первая, где он появился, поэтому это её ответственность закрыть цепочку. Проверено вживую: фильтр «Прил.» → сессия только из прилагательных; `być\|VERB` (не due) → пустая scoped-очередь → EmptyState |
| 2026-09-02 | 13 | Реестр компонентов упражнений — не буквальный `Record<Exercise['type'], ComponentType<ExerciseProps<...>>>`, а фабрика `createExerciseComponentRegistry({srsState, now})` | `SelfAssessExercise` (задача 12) требует доп. пропсы `srsState`/`now` сверх базового `ExerciseProps<E>` — прямое присваивание в `Record<...>` не тайпчекалось (`intersection reduced to never`). Фабрика замыкает слот `self-assess` на текущий сессионный контекст, остальные слоты — без изменений |
| 2026-09-02 | 13 | Создан `src/db/repositories/sessions.repository.ts` — не существовал ни в одной из задач 01-12 | Табличка `sessions` в Dexie-схеме заведена задачей 05, но ни одна задача до 13 не писала в неё; восстановление сессии (§5 задачи 13) — первое реальное использование |
| 2026-09-02 | 13 | **Найден и исправлен баг во время браузерной проверки агентом**: при построении пустой очереди `startFresh()` создавал строку `sessions` ДО проверки на пустоту, оставляя вечно «незавершённую» сессию-сироту без единого ответа | Следующий заход на `/session` ошибочно предлагал «продолжить» пустышку вместо построения нового запроса. Исправлено: `sessionId` создаётся только когда план гарантированно не пуст; ресьюм, ставший пустым, закрывается через `completeSession`, а не бросает |
| 2026-09-02 | 13 | Восстановление сессии не хранит исходный `filter`/`wordId`-scope в `SessionRecord` — при resume очередь строится заново по ТЕКУЩЕМУ `location.state` (обычно глобальному после перезагрузки, т.к. router state не переживает жёсткую перезагрузку страницы) | Задокументировано в коде как известное ограничение; исключение уже отвеченных навыков всё равно работает через `reviewLogs` этой сессии |
| 2026-09-02 | 13 | Супервизор проверил вживую и подтвердил: ручная проверка клавиши Enter/Space через инструмент браузера не срабатывала на кнопке «Далее», НО это оказался артефакт синтетических key-событий самого инструмента (пустой `e.key`, и та же проблема воспроизводится на несвязанной кнопке «Фильтры» на `/words`) — не баг приложения. `@testing-library/user-event` в существующих тестах (`ExerciseFeedback.test.tsx`, `InputExercise.test.tsx`) корректно подтверждает активацию по Enter | Для будущих задач: не полагаться на ручную проверку Enter/Space через Browser-инструмент этой сессии — использовать RTL/`userEvent` тесты как источник истины для клавиатурной доступности |
| 2026-09-02 | 14 | **Исправлен реальный пробел задачи 03**: `SessionMode` был `'learn'\|'practice'`, без `'mistakes'`, хотя architecture.md §10 явно требует третье значение. Расширено до `'learn'\|'practice'\|'mistakes'`; `shouldApplySrs(isFirstAnswerInSession, mode)` теперь всегда `false` для `mode === 'mistakes'` — строже правила 1, т.к. в разборе ошибок нет осмысленного «первого ответа в сессии» (пользователь только что видел верный ответ) | FR-103: «Пользователь только что видел правильный ответ — успех здесь ничего не доказывает». Подтверждено супервизором вживую: FSRS-поля `SkillRecord` (state/stability/difficulty/due/reps/lapses/lastReviewAt) побайтово не изменились после верного ответа в mistakes-сессии, `reviewLogs.srsApplied: false` |
| 2026-09-02 | 14 | `resolveMistakeScope(skillIds)` — четвёртый вид scope (наряду с global/filter/word из задачи 13): берёт навыки НАПРЯМУЮ по `getSkill`, без due-фильтрации, без новых слов; router state `{skillIds}` для «Разобрать ошибки» проверяется в `parseSessionScope` раньше `wordId`/`filter` | Разбор ошибок — не Learn-очередь по расписанию, а фиксированный список конкретных навыков, на которых пользователь ошибся именно в этой сессии |
| 2026-09-02 | 14 | Сессия с нулём ответов не помечается `completeSession`, а удаляется через новый `deleteSession` | Буквальное «не создаёт мусорную запись» (acceptance п.8) требует отсутствия строки, а не строки с `endedAt` и нулевыми счётчиками; подтверждено супервизором вживую — сессия-сирота реально отсутствует в таблице `sessions` после выхода с нулём ответов |
| 2026-09-02 | 14 | `dailyStats.sessionsCount` — поле было объявлено в схеме с задачи 03/05, но нигде не инкрементировалось; добавлен инкремент в `completeSession`, по локальному дню `endedAt` (не `startedAt`) | Корректное поведение на границе полуночи — сессия, начатая до полуночи и законченная после, должна засчитаться следующему дню |
| 2026-09-02 | 14 | Заголовочный процент результатов и список «Ошибки»/«Сложнее всего» используют разные определения «верно»: заголовок засчитывает `nearMiss` как верный (как и раньше в живой сессии), список ошибок и точность по измерениям — строгий `log.correct` | near-miss может попасть в список «на разбор», не портя итоговый процент — сохраняет уже существующую в кодовой базе семантику near-miss (`grade()`), не вводит новое правило |
| 2026-09-02 | 15 | Заголовок страницы — «Главная», а не «Dzisiaj» из макета app-design §2; «Dzisiaj» перенесено в подпись под заголовком | У всех остальных страниц `<h1>` совпадает с подписью в нижней навигации (Слова/Практика/Прогресс/Настройки), это уже проверяется существующими тестами (`App.test.tsx`, `router.test.tsx`); сохранена стилистика макета без разрушения устоявшегося соглашения |
| 2026-09-02 | 15 | «Изучается»/«выучено» — непересекающиеся корзины: `learning` = изучается, `known ∪ mastered` = выучено (не кумулятивно «начато ⊇ закончено») | Так числа независимо проверяются фильтром статуса на `/words` без двойного счёта; подтверждено супервизором вживую: «6 изучается» на главной = «Найдено 6» на `/words` с фильтром `status=Изучаю` |
| 2026-09-02 | 15 | Подсчёт по разделам (POS) — `db.wordProgress.where('status').equals(...).primaryKeys()` (три параллельных индексных запроса на known/mastered/learning), числитель по POS считается декодированием `wordId` (`decodeWordId`) уже отфильтрованных по индексу primary keys, знаменатель — из `getIndexStore().byPos` (синхронный in-memory индекс контента, задача 04) | Буквально удовлетворяет acceptance п.8 «через индексы, не через загрузку всех записей»: `.primaryKeys()` не десериализует сами записи и не сканирует таблицу целиком, а `byPos` — вообще не Dexie-запрос |
| 2026-09-02 | 15 | «Открыть» и строки разделов (Существительные/Глаголы/Прилагательные) ведут на `/words` с фильтром по POS через `filters.store`, а не на `/nouns`/`/verbs`/`/adjectives` | Эти маршруты — пустые заглушки задачи 06; согласно architecture.md §9 и задаче 07, части речи открываются переключателем ВНУТРИ «Слова», а не отдельными разделами |
