import { PageContainer } from '@/components/app/PageContainer.tsx'
import { PageHeader } from '@/components/app/PageHeader.tsx'

/** Stub — manual practice-mode setup (`training-setup` feature) is a later task. */
export function PracticeSetupPage() {
  return (
    <PageContainer>
      <PageHeader title="Практика" description="Ручной режим тренировки — задача 07+." />
    </PageContainer>
  )
}

export default PracticeSetupPage
