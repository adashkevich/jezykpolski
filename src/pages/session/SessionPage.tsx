import { PageContainer } from '@/components/app/PageContainer.tsx'
import { PageHeader } from '@/components/app/PageHeader.tsx'

/** Stub — the shared Learn/Practice exercise runner (`architecture.md` §3, §10) is a later task. */
export function SessionPage() {
  return (
    <PageContainer>
      <PageHeader title="Сессия" description="Раннер упражнений — задача 07+." />
    </PageContainer>
  )
}

export default SessionPage
